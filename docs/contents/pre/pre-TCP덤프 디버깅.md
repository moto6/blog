## TCP 덤프로 디버깅
- “네트워크 이슈가 발생하면 먼저 tcpdump로 문제 구간의 패킷을 캡처합니다. 
0 이후 Wireshark 등으로 분석해서 SYN/SYN-ACK 흐름이나 Retransmission, RST 여부를 확인해 연결 단계에서 문제가 있는지 판단합니다.
- DNS, TLS handshake, 애플리케이션 레벨까지 단계별로 어디서 지연이나 실패가 발생하는지 좁혀갑니다. 
- 마지막으로 정상 케이스와 비교해서 차이를 기반으로 네트워크, 방화벽, 애플리케이션 중 원인을 특정합니다.”
